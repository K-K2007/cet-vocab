# 词驿 · EdgeOne Pages 部署指南

## 部署物清单

| 文件 | 说明 |
|---|---|
| `index.html` | 应用全部代码（单文件，约 1.2MB，CDN gzip 后约 300KB） |

无构建步骤、无外部本地依赖。页面引用的外部资源仅 Google Fonts（已做异步加载，大陆网络不可达时自动回退系统衬线字体，不阻塞渲染）。

## 方式一：Git 仓库部署（推荐，自动更新）

1. 将 `cet-vocab/` 目录作为仓库根（或把 `index.html` 放在仓库根目录）推送到 GitHub / Gitee / CODING 等平台。
2. 打开 [EdgeOne Pages 控制台](https://console.cloud.tencent.com/edgeone/pages)，点击 **创建项目** → **导入 Git 仓库**。
3. 授权并选择仓库后配置构建设置：
   - **框架预设**：`None`（纯静态）
   - **构建命令**：留空
   - **输出目录**：`/`（若 index.html 在仓库根）；若在子目录如 `cet-vocab/`，填 `cet-vocab`
4. 点击 **开始部署**，完成后获得 `*.edgeone.app` 默认域名，可直接在手机浏览器打开。

## 方式二：直接上传

控制台创建项目时选择 **上传本地文件**，把 `index.html` 拖入即可（适合无 Git 仓库时快速验证）。

## 绑定自定义域名（可选）

项目 → **域名管理** → 添加域名 → 按提示到域名 DNS 服务商添加 CNAME 解析记录 → 等待证书签发（EdgeOne 自动提供 HTTPS）。

## 部署后自检清单

- [ ] 手机浏览器打开，首页正常渲染（无白屏；字体加载慢时直接显示系统字体）
- [ ] 点「开始背单词」→ 双选自评 → 提示 → 看答案 → 详情，全流程可用
- [ ] 点单词上的喇叭图标能朗读（需要系统含英语语音包；iOS 首次点按后即可发声）
- [ ] 学习几个词后刷新页面，进度仍在（IndexedDB 在 https / localhost 下可用）
- [ ] 个人中心 → 词书管理：10 本内置词书可见、切换正常
- [ ] 学习计划弹层：选词书、调每日词数，刷新后设置保留
- [ ] iOS 刘海屏：顶栏不被状态栏遮挡，底部按钮不被 Home 指示条遮挡

## 注意事项

- **必须 HTTPS**：EdgeOne Pages 默认提供 HTTPS；网页语音合成（speechSynthesis）与 IndexedDB 在 http 非 localhost 下部分浏览器会受限。
- **数据存本地**：学习进度保存在浏览器 IndexedDB 中，换设备/清缓存不互通；可在个人中心「导出备份」生成 JSON，在新设备「导入备份」恢复。
- **回滚**：Git 部署模式下每次提交自动生成历史版本，控制台可一键回退。
